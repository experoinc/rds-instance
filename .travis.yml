---
language: python
python:
- '2.7'
install:
- 'pip install yamllint'
- 'pip install cfn-lint'
script:
- 'yamllint module.yml'
- 'cfn-lint -i E2520 -t module.yml ' # TODO false positives in rule E2520
- 'grep -q "LICENSE-2.0" module.yml'
